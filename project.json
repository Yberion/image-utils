{
  "name": "bf-image-utils",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/bf-image-utils",
        "main": "./src/index.ts",
        "tsConfig": "./tsconfig.lib.json",
        "assets": ["*.md"]
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["./**/*.ts", "./package.json"]
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["coverage/bf-image-utils"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "coverage/bf-image-utils"
      }
    }
  },
  "tags": []
}
